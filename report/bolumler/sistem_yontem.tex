\chapter{Sistem ve Yöntem}
\label{ch:sistem-yontem}

\section{Donanım ve Araçlar}

Tüm cihaz-üzeri ölçümler \textbf{NVIDIA Jetson AGX Orin}~\cite{nvidia2024orin}
üzerinde alınmıştır. Orin'in CPU ve GPU'su tek bir paylaşımlı bellek
baytı ve bant genişliği üzerinden çalışır. Inference motorları
\textbf{TensorRT}~\cite{nvidia2024tensorrt} ile derlenmiş, fp16
hassasiyetinde ve CUDA Graph tekrar oynatmasıyla çalıştırılmıştır.

\begin{table}[H]
  \centering
  \caption{Yazılım/donanım ortamı.}
  \label{tab:ortam}
  \begin{tabular}{ll}
    \toprule
    Bileşen & Sürüm \\
    \midrule
    GPU compute capability (SM) & 8.7 \\
    Güç modu & MAXN, \texttt{sudo jetson\_clocks} \\
    TensorRT & 10.3.0 \\
    PyTorch (CUDA) & 2.8 (CUDA 12.6) \\
    torchvision & 0.23.0 \\
    ONNX / onnxruntime & 1.22.0 / 1.23.2 \\
    OpenCV / NumPy & 4.11.0 / 1.26.4 \\
    Python & 3.10.12 \\
    Motor hassasiyeti & fp16 \\
    \bottomrule
  \end{tabular}
\end{table}

\section{Kare Başına Hesaplama ve Dört Motor Kararı}
\label{sec:dort-motor}

\Cref{sec:proje-eslesme}'de gösterildiği gibi kare başına dört modül
çalışır: image encoder, memory attention, memory encoder, SAM head.
Bunların \emph{neden} tek bir birleşik TensorRT motoru değil de dört
ayrı motor olarak bırakıldığı gelişigüzel bir karar değildir; nedeni
ONNX'in çalışma biçimidir. Bir PyTorch modeli ONNX'e aktarılırken
\emph{trace} edilir: model bir örnek girdiyle bir kez çalıştırılır,
yalnızca o çalışmada gerçekleşen tensör işlemleri kaydedilir. Python'un
kendisi (bir \texttt{if}, bir sözlük araması, çağrılar arası saklanan
bir durum) bu kayda hiç girmez: \texttt{if} dallanması export anında
hangi koldan geçildiyse o kola sabitlenir, durum yalnızca export
anındaki değeriyle donar. TensorRT bu sabit grafiği derler, ONNX'in
kaydetmediği hiçbir kararı veremez; bu yüzden her motorun sınırı, bir
sonraki adımda hangi verinin kullanılacağına dair bir Python kararının
bulunduğu yerdedir. Modüller, aralarına \textbf{Python'un girdiği}
noktalardan ayrılmıştır:

\begin{itemize}
  \item \textbf{image encoder → memory attention} arasında memory bank
        derlenir. Bu banka kare indeksiyle anahtarlanmış bir Python
        \texttt{dict}'tir; hangi geçmiş karelerin okunacağı her karede
        \texttt{.get()} çağrısı ve en yakın adayları sıralayan bir
        fonksiyonla (\texttt{select\_closest\_cond\_frames}) belirlenir.
        Bir TensorRT motorunun ne \texttt{dict}'i, ne \texttt{.get()}'i,
        ne de veriye bağlı bir dallanması vardır.
  \item \textbf{SAM head → memory encoder} arasında bu karenin maskesi
        aynı \texttt{dict}'e geri yazılır.
  \item \textbf{memory attention → SAM head} arasında ise hiçbir şey
        yoktur; bu sınır zorunlu değildir.
\end{itemize}

Değişken uzunluklu bellek sorunu, sabit 7 yuvaya padding ve toplamsal
bir dikkat maskesiyle işgal durumunu taşıma yoluyla çözülmüştür. Motora
taşınamayan şey, \emph{hangi} karelerin bu yuvaları dolduracağına karar
vermektir, çünkü bu karar bir sözlük sorgusu ve bir sıralamadır. Tek
motora birleştirmenin ilke olarak mümkün olup olmadığı ve buna neden
gerek görülmediği \cref{sec:fuzyon-karari}'de ele alınmaktadır.

\section{Sabit Şekilli Memory Bank ve CUDA Graph}

Bir CUDA Graph, kaydedildiği kernel çağrılarını \emph{aynı} cihaz
adreslerine karşı tekrar oynatır; bu yüzden her motor kalıcı arabellekler
tutar, bir kez ısınıp yakalanır, ve o andan sonra her kare yalnızca bir
kopyalama ve bir tekrar oynatmadan ibarettir.\footnote{Adresler
TensorRT'e bir kez verilir, bağlam ilk \texttt{enqueue} çağrısında
ısıtılır, sonraki kareler yalnızca \texttt{graph.replay()} çağırır.}

Bunun çalışabilmesi için memory bank'ın \textbf{sabit şekilli} olması
gerekir: gerçek checkpoint'te anahtar uzunluğu ilk 23 karede 16 farklı
değer almıştır.\footnote{\texttt{tools/analyze\_cost.py
--trace-frames 24}: 1. karede 516, 7. karede 3612, 16. kareden sonra
sabit 3648 belirteç.} Çözüm, 7 sabit yuvalı bir arabellek artı
toplamsal bir dikkat maskesidir: boş yuvalar $-30000$ ile maskelenir,
softmax'ı canlı önek üzerinde hesaplananla eşdeğer kılar.\footnote{$\exp(-30000)$
hem fp16 hem fp32'de 0'a taşar; sonlu bir değer seçilmesinin nedeni bir
satırın tamamen maskelenmesi durumunda NaN üretmemektir.} Doldurmanın
zararsız olması, RoPE'nin yuva-konumundan bağımsız olmasından ve
zamansal konumun zaten \texttt{memory\_pos} içine gömülü olmasından gelir.

\section{Girdi Çözünürlüğü: 1024, 768, 512}
\label{sec:cozunurluk}

Checkpoint 1024'te eğitilmiştir, ama girdi çözünürlüğü kodda tek bir
alandır --- \texttt{image\_size} --- ve türetilen bütün şekiller
ondan çıkar. Bu, çözünürlüğü serbest bir ayar yapmaz: bir sayının geçerli
\emph{olabilmesi} için sağlaması gereken koşullar vardır. 768 üçüncü bir
nokta olarak eklenmiştir çünkü bu koşulların hepsini sağlar.

Farklı bir boyutun yüklenip çalışabilmesinin nedeni, backbone ve neck'in
tümüyle evrişimsel olması ve konum kodlamalarının kare başına
hesaplanmasıdır. Kendini ayarlamayan tek bir tablo vardır: memory
attention'ın cross-attention'ı (\texttt{RoPEAttentionv2}) döner
tablolarını kurulum anında sabit bir \texttt{q\_sizes} değerinden
üretir; 1024 için bu \texttt{[64,\,64]}'tür.
\texttt{src/trackers/\_hydra\_overrides.py} bunu
\texttt{image\_size / backbone\_stride} ile kilitli tutar; bu olmadan
export ``Query RoPE table covers 4096 tokens but the feature map has
1024'' hatasıyla düşer.

\begin{table}[H]
  \centering
  \caption{Bir çözünürlüğü geçerli kılan koşullar ve 768'in bunları
    nasıl sağladığı. Buradaki hiçbir sayı ölçüm değildir; hepsi
    mimariden çıkan aritmetiktir.}
  \label{tab:cozunurluk-768}
  \small
  \begin{tabular}{@{}>{\raggedright\arraybackslash}p{72mm}>{\raggedright\arraybackslash}p{72mm}@{}}
    \toprule
    Koşul & 768'de \\
    \midrule
    stride-2 zincirleri (trunk, neck, mask downsampler) tam bölünmeli &
      $768 = 3\cdot2^{8}$ --- sekiz tam yarılama, modeldeki her
      zincirden derin \\
    \texttt{image\_size} \% \texttt{backbone\_stride} &
      $768/16 = 48$ \\
    self-attention belirteç sayısı tam kare olmalı & $48^{2} = 2304$ \\
    SAM head yüksek çözünürlük seviyeleri (stride 8 ve 4) & 96 ve 192 \\
    hafıza tarafı (\texttt{k\_sizes}) & değişmez --- sabit
      $16{\times}16$ latent ızgara, \texttt{image\_size}'ın fonksiyonu
      değil \\
    \bottomrule
  \end{tabular}
\end{table}

$512 = 2^{9}$ ve $1024 = 2^{10}$ aynı testlerden geçer;
\textbf{720 geçmez} ($720/16 = 45$ ve ikiye bölme orada tam olmaktan
çıkar). \texttt{tests/test\_hydra\_overrides.py} bu aritmetiği depodaki
her config üzerinde doğrular, yani ileride eklenecek bir
\texttt{edgetam\_720.yaml} export anında değil CI'da düşer.

\textbf{Maliyet, belirteç sayısının koyduğu yerdedir.} 768 kare başına
2304 uzamsal belirteç üretir; 1024'te 4096, 512'de 1024. Yani 1024'ün
uzamsal işinin \%56'sı. 512 konfigürasyonuna göre image encoder piksel
sayısıyla ölçeklenir (2,25×), memory attention'ın self-attention'ı
belirteç sayısının karesiyle ($\approx$5×), sabit hafıza bankasına olan
cross-attention'ı ise belirteç sayısıyla (2,25×).

\textbf{768'in yeri olmadığı durum.} Kaynak 768'in altındaysa 768
\emph{yukarı ölçekler}: örneğin 640×512 bir kayıtta, yeni hiçbir bilgi
taşımayan ara değerlenmiş pikseller için 512'nin 2,25 katı belirteç
ödenir ve 6 pikselik bir hedef 9 piksele çıkar --- hâlâ tek bir
stride-16 hücresinin içinde. Orada doğru araç
\texttt{src/trackers/adaptive.py}'dir: 512 fiyatına 4--10× büyütme
verir. 768, gerçekten 512×512'den fazla detay taşıyan kaynaklar
içindir; bu projede 1280×720 kayıtlar.

\textbf{Durum: configler ve testler hazır, ölçüm yok.} Hazır olanlar
\texttt{configs/edgetam\_768.yaml} ve
\texttt{configs/edgetam\_trt\_768.yaml}; 768'de henüz hiçbir motor
derlenmedi ve hiçbir ölçüm alınmadı. İlk gerçek kontrol
export'un kendi koruması olacaktır: \texttt{\_CrossAttentionGraph},
\texttt{q\_sizes} ile öznitelik haritası uyuşmazsa kurulurken hata
verir.

\section{Ölçüm Metodolojisi}
\label{sec:olcum-yontemi}

Gerçek zamanlı bütçeye giren üç aşama, girmeyen bir aşama vardır:

\begin{table}[H]
  \centering
  \caption{Bir karenin zamanının bileşenleri.}
  \label{tab:kare-bilesenleri}
  \begin{tabular}{lll}
    \toprule
    Aşama & Ne & Bütçede mi? \\
    \midrule
    \texttt{pre} & kırpma + model girdisine ölçekleme + normalize etme & Evet \\
    \texttt{inference} & dört modül + EdgeTAM'ın Python defter tutması & Evet \\
    \texttt{post} & maskeleri kaynak çözünürlüğe geri ölçekleme & Evet \\
    \texttt{read} & kare dosyasını diskten okuma/çözme & \textbf{Hayır} \\
    overlay + mp4 & maskeyi çizip video kodlama & \textbf{Hayır} \\
    \bottomrule
  \end{tabular}
\end{table}
