# EdgeTAM CPU testing config — slower; for development on a machine without
# CUDA. On Orin AGX use configs/edgetam.yaml (the default).
model_cfg: configs/edgetam.yaml
checkpoint: third_party/EdgeTAM/checkpoints/edgetam.pt
device: cpu
fp16: false
mask_threshold: 0.0
